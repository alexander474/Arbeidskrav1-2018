public class Main {

    // running the "program"
    public static void main(String[] args) {
        Client cl = new Client();
        //cl.mainMethod(); // running the mainMethod in the Client class
        javafx.application.Application.launch(Gui.class); // running the Gui class
    }

}
